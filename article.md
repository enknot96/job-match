## 1. はじめに 〜 なぜ作ったか

つくってみようと思ったきっかけは、**RAGとかベクトル検索とか、前から気になってたから**です。

「そろそろちゃんと触ってみたいなぁ、まずは定番の pgvector あたりから勉強しよかな」と思ってたところに、今回のお題（TiDBでAI時代のデータ基盤）を見つけて、「お、これちょうどええ寄り道やん」と参戦を決めました。そして、Zenn T-shirtも欲しい🔥

で、せっかくなら**自分の身近な業務**を題材にしたいなと。

ぼくの会社では、求人に応募があると、たぶん多くの会社と同じで **応募者の履歴書をAI（Gemini）に読ませて「ウチに合いそうか？」をざっくり見る**、みたいなことをやっています。便利なんですが、ふと思いました。

> 応募がまとまって来たとき、1人ずつ読ませるより、**「要件に合う順」でランキングできたらラクなのでは？**

やってることの本質は同じ（**履歴書 × 求人要件のマッチ度を測る**）で、それを「1人ずつ」から「複数まとめて並べる」に広げてみたら仮にどうなるんやろ？という**好奇心＋実験**です。**本番投入じゃなく、勉強も兼ねたPoC**ですな。

あいも変わらず、Claude Code 様にもお助けいただき、AmiVoice(様)の時同様、最初に「何を作るか・どういう構成にするか」の**定義（仕様）を一緒に固めて**から、**コードは基本自分で手を動かして、分からん所はその都度『これどういう意味？』と聞いて理解しながら**進めました。背伸びはせず、自分の理解で進めるスタンス！

おこがましいですが、この記事で共有したい知見はだいたいこの3つ。

1. **1つのDB・1つのテーブル**で「ベクトル検索（意味）」と「全文検索（キーワード）」を同居させ、**ハイブリッド検索**する話（TiDBならでは）
2. 2つの検索結果を **RRF** で“順位”だけ使って安全に融合する話
3. 2026年スタック（Next.js 16 / Vercel AI SDK v6 / Gemini）と**TiDBのハマりどころ**

---

## 2. 何ができたか（完成物デモ）

**求人要件を入れる → 応募者をAIが“合う順”にランキング → なぜ合うのかAIがコメント**、というツールです。まずは動いてるところをどぞどぞ。

![（スクショ）ビフォーアフター比較画面：左「ベクトル検索のみ」／右「ハイブリッド検索」](ここに画面全体のスクショ)

> 求人要件とキーワードを入れると、左右に「ベクトル検索だけ」と「ハイブリッド検索」の順位が並びます。各カードの🤖ボタンでAIが推薦理由を喋ります。

:::message
このデモは個人の無料枠（TiDB Cloud / Gemini / Vercel）で動かしています。

- AI（Gemini）やDB（TiDB）の**無料枠の上限に達していると、一時的に使えない**ことがあります。
- データはすべて**ダミーの応募者**です（個人情報は使っていません）。
- コード例と動作イメージは記事に残すので、雰囲気はそちらで掴んでもらえれば！
  :::

---

## 3. ハイブリッド検索とは / なぜ必要か

採用マッチングを考えると、外せないものが2つあります。**意味の近さ**と、**キーワードの正確さ**。

たとえば「Reactのフロントエンドエンジニア。**AWS認定必須**」で応募者を探すとき——

- **ベクトル検索（意味）だけ**だと：「React」と書いてなくても「Next.js + TypeScript でSPA開発」な人を“近い”と拾える。けど「**AWS認定**」みたいな「**この語が入ってること自体が条件**」のやつを取りこぼしがち。埋め込みは語を意味に溶かしちゃうので、資格名・固有名詞・型番の厳密一致が苦手。
- **全文検索（キーワード）だけ**だと：「AWS認定」はバッチリ拾える。けど「React 3年以上」で「Next.js 2年」の人が引っかからない。意味の近さは分からん。

この2つ、**弱点がちょうど正反対**なんですよね。なら**合体させたら互いの穴を埋め合える**やん、というのがハイブリッド検索です。RAGの検索品質を上げる定番アプローチでもあるそう。

「なるほど、なるほど」（←この記事で何回言うんやろ）

---

## 4. なぜ TiDB？（正直ベースで）

で、ここがTiDBの面白いとこでした。
**ベクトル検索も全文検索も、同じテーブル・同じSQLでできる**んです。

ぼく、最初これにビックリしました。「ベクトルDB（pgvectorとか）と検索エンジン（Elasticsearchとか）を**別々に立てて繋ぐ**もんやろ？」と思ってたので。

ただ、正直に書いておくと——調べたら「ベクトル＋全文を1テーブル」自体は、実は **Postgres + pgvector + 標準の全文検索** でも一応できるし、MySQLにもベクトル型はあります。なので「TiDBじゃないと無理」ではないです。

じゃあTiDBの何が嬉しいかというと、**「継ぎ足し・別システムの並走なしで、最初からベクトル・全文・SQLが揃っていて、1クエリで融合できる」**こと。拡張を入れたり別エンジンを運用したりせず、マネージドな1つのDBで完結する。しかもサーバーレスで**無料枠がそこそこ手厚い**。学習＆PoC用途にはドンピシャでした。

---

## 5. 構成・技術選定

スタックはこんな感じ。

| レイヤー             | 使ったもの                              | 役割                             |
| -------------------- | --------------------------------------- | -------------------------------- |
| フロント / API       | Next.js 16 (App Router)                 | 画面とAPIルート                  |
| AI（埋め込み・生成） | Vercel AI SDK v6 + Google Gemini        | ベクトル化＆推薦コメント         |
| 意味検索             | TiDB `VEC_COSINE_DISTANCE`（HNSW索引）  | セマンティックな近さ             |
| キーワード検索       | TiDB `FTS_MATCH_WORD`（BM25・全文索引） | 厳密一致・資格名                 |
| スコア融合           | RRF（Reciprocal Rank Fusion）           | 2つの順位を合体                  |
| DB                   | TiDB Cloud Starter                      | ベクトル＋全文＋SQLを1クラスタで |
| デプロイ             | Vercel                                  | フロント＋API                    |

AIは無料枠が手厚い **Gemini** をチョイスしました。理由は3つで、**無料枠が潤沢／日本語に強い／そもそも業務でGem使ってる地続き感**。AI SDKはプロバイダを差し替えるだけなので、あとからOpenAI等にも変えられて気楽です。

なお後述しますが、**TiDBのクラスタは（全文検索の都合で）シンガポールリージョンで作る**のがポイントです。

---

## 6. 実装（核心）

### スキーマ：1テーブルに「2つの検索エンジン」を作る

応募者テーブルに、ベクトル用カラムと、2種類の索引を作ります。

```sql
CREATE TABLE candidates (
  id          BIGINT AUTO_INCREMENT PRIMARY KEY,
  name        VARCHAR(255) NOT NULL,
  resume      TEXT         NOT NULL,
  embedding   VECTOR(1536),                                    -- ★ベクトル用カラム
  created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  VECTOR INDEX idx_embedding ((VEC_COSINE_DISTANCE(embedding))) -- ★①意味検索エンジン(HNSW)
);

-- ★②キーワード検索エンジン(全文索引・BM25)。日英混在に強いMULTILINGUALパーサ
ALTER TABLE candidates
  ADD FULLTEXT INDEX idx_resume_fts (resume)
  WITH PARSER MULTILINGUAL ADD_COLUMNAR_REPLICA_ON_DEMAND;
```

`VEC_COSINE_DISTANCE` も `FTS_MATCH_WORD` もTiDB標準の関数で、それを高速化する索引（`VECTOR INDEX` / `FULLTEXT INDEX`）を自分で張る、という構図です。

### 履歴書を Gemini でベクトル化

```ts
// lib/embed.ts
import { google } from "@ai-sdk/google";
import { embed } from "ai";

const embeddingModel = google.embedding("gemini-embedding-001");

export async function embedText(text: string): Promise<number[]> {
  const { embedding } = await embed({
    model: embeddingModel,
    value: text,
    // 既定は3072次元。VECTOR(1536) に合わせて1536へ縮約（Matryoshka）
    providerOptions: { google: { outputDimensionality: 1536 } },
  });
  return embedding;
}
```

### ①意味 → ②キーワード → ③RRFで融合

検索APIの中身です。

```ts
// app/api/match/route.ts（抜粋）
const vecStr = `[${(await embedText(jobDescription)).join(",")}]`;
const ftsText = (keywords && keywords.trim()) || jobDescription;

// ① ベクトル検索（意味の近さ）
const [vecRows] = await db.execute(
  `SELECT id FROM candidates
   ORDER BY VEC_COSINE_DISTANCE(embedding, ?) ASC
   LIMIT 20`,
  [vecStr],
);

// ② 全文検索（キーワード/BM25）※ FTSは db.query で（理由は後述）
const [ftsRows] = await db.query(
  `SELECT id FROM candidates
   WHERE FTS_MATCH_WORD(?, resume)
   ORDER BY FTS_MATCH_WORD(?, resume) DESC
   LIMIT 20`,
  [ftsText, ftsText],
);

// ③ RRF融合：各リストで 1/(K + 順位) を、同じ人に足し込む
const K = 60;
const score = new Map<number, number>();
vecIds.forEach((id, i) => score.set(id, (score.get(id) ?? 0) + 1 / (K + i + 1)));
ftsIds.forEach((id, i) => score.set(id, (score.get(id) ?? 0) + 1 / (K + i + 1)));
```

RRFは正直、中の数式を完全に理解する必要はなくて、**「順位を `1/(60+順位)` に変換して足すと、スケールの違う2つの検索結果をいい感じに混ぜられる」出来合いのレシピ**くらいの理解で使えます。ミソは `?? 0`。**両方の検索に出てきた人は点が2回足されて高得点**になる＝「意味でもキーワードでも当てはまる人」がトップに来る、というのが効きどころです。

### Gemini に「なぜこの人？」を喋らせる

上位の応募者について、推薦コメントをストリーミング生成。

```ts
// app/api/explain/route.ts（抜粋）
const result = streamText({
  model: google("gemini-2.5-flash-lite"),
  system: "採用担当者向けに、なぜこの求人にマッチするかを150字以内で簡潔に。誇張せず事実ベースで。",
  prompt: `求人要件: ${jobDescription}\n応募者: ${candidate.name}\n経歴: ${candidate.resume}`,
});
return result.toTextStreamResponse(); // AI SDK v6 は生テキストならこれ
```

### 比較UI

画面では **「ベクトル検索のみ」と「ハイブリッド」を左右に並べて**、どっちで拾われたか（意味/キーワード）をバッジで出すようにしました。これでハイブリッドの効きが一目で分かります。

---

## 7. ハマりどころコラム

初挑戦だらけだったので、つまずきポイントを置いときます。

### 「ベクトル」は `[...]` の“文字列”で渡す

TiDBにベクトルを保存するとき、配列をそのままじゃダメで `'[0.1,0.2,...]'` という**文字列**にして渡します。

```ts
const vecStr = `[${vector.join(",")}]`; // ← 配列を [...] の“文字列”に
```

MySQLのプロトコルに“ベクトル型”が無いので、文字列に詰めて送ってTiDB側で復元するんやな、と理解しました（pgvectorも同じ作法らしい）。「ベクトルって結局ただの数値の羅列なんやな」と腹落ちした瞬間でした。

### 全文検索は `db.execute` じゃなく `db.query`

②の全文検索で、最初これが出て悩みました。

```
ERROR 1235: This version of TiDB doesn't yet support 'match against a non-constant string'
```

`FTS_MATCH_WORD` の検索語は**“定数の文字列”じゃないとダメ**らしく、`db.execute`（サーバー側プリペアド）だと `?` が「非定数」扱いで弾かれます。全文検索だけ **`db.query`**（値をクライアント側で埋め込む）に変えたら通りました。「ベクトルは execute、全文は query」。地味だけど確実に踏むやつ。

### 全文検索が使えるリージョンが限られてる（東京ダメ）

これが一番ハマりました。TiDBのネイティブ全文検索は**まだベータで、2026年6月時点だと AWS フランクフルト / シンガポールだけ**。何も考えず東京で作ると `FTS_MATCH_WORD` が動きません（＝ハイブリッドの片肺が死ぬ）。日本から近い**シンガポール**で作り直しました。「東京ちゃうんかーい」

### 1テーブルに2索引が“同居”する驚き

最初に書いたとおりですが、`SHOW CREATE TABLE` すると1つのテーブルに `VECTOR INDEX` と `FULLTEXT INDEX` が両方ぶら下がってます。**意味検索エンジンとキーワード検索エンジンが1テーブルに同居**してるの、初見だと結構な「へぇ！」でした。

| 症状                              | 直し方                                                                    |
| --------------------------------- | ------------------------------------------------------------------------- |
| `ERROR 1235 non-constant string`  | 全文検索は `db.execute`→**`db.query`**                                    |
| `FTS_MATCH_WORD` がそもそもエラー | リージョンがFTS非対応（東京等）。**シンガポール**で作る                   |
| ベクトルのINSERTがコケる          | 配列ではなく **`'[...]'` 文字列**で渡す                                   |
| 次元が合わない                    | Geminiは既定3072。**`outputDimensionality: 1536`** で合わせる             |
| 画面の文字が白くて見えない        | create-next-appの**ダークモードCSS**衝突。`globals.css`のダーク指定を外す |

毎度のことながら「**一次情報を調べろ**」を地で学ぶプロジェクトでした。

---

## 8. 動かしてみた結果（…いや待てよ、から始まる）

最初、応募者を**7人**だけ入れて検索したら、**上位が全員「意味ヒット＆キーワードヒット」**になっちゃって。

「これ、ベクトルだけでもキーワードだけでも同じ結果なんちゃう？ ハイブリッドにした意味なくない…？」

で、整理して気づきました（正直、Claude Codeに整理してもらって腹落ちした）。**応募者が7人しかいないと、ベクトル検索の `LIMIT 20` で全員が入っちゃう**んですね。だから差が出ない。

なので応募者を**30人**に増やして、「フロント強いけど認定なし」「バックエンドだけど認定持ち」みたいな人を混ぜて、もう一回。すると、ちゃんと**“両方向の救済”**が観測できました。

| 名前                   | 意味ヒット | キーワードヒット | 何が起きたか                                            |
| ---------------------- | ---------- | ---------------- | ------------------------------------------------------- |
| 橋本さん（Angular/TS） | ✅         | ❌               | Reactじゃないのに意味的に近く、**ベクトルだけが拾った** |
| 森さん（SRE/AWS認定）  | ❌         | ✅               | インフラ職で意味的に遠く、**キーワードだけが拾った**    |

つまり：

- **ベクトル検索だけ**なら → 森さん（AWS認定保持者）を**取りこぼす**
- **キーワード検索だけ**なら → 橋本さん（実質Reactレベルのフロント）を**取りこぼす**
- **ハイブリッドだから両方拾えた** ✅

![（スクショ）30人時点：左(ベクトルのみ)に森さんが居ない／右(ハイブリッド)に森さんが復活](ここにビフォーアフターのスクショ)

「データが少なすぎて意味なく見えてただけ」やったんですね。ここが今回いちばん「なるほど〜」となったポイントでした。

---

## 9. 正直な話（限界とコスト）

良いことばかりじゃないので、正直に。

- **FTSはベータ＆リージョン制約**：前述のとおり、対応リージョンでしか動かない。本番採用なら要確認。
- **履歴書まるごと1ベクトルの限界**：長い経歴だと意味が薄まる。本当は**チャンク分割**すべき。今回は簡易版なので割り切り。
- **RRFは素のまま**：重み付け融合やリランカーで精度は上げられる。まずRRFで動かす、が入口としてラク。

コスト面は、結論**実質ほぼ$0**で動きます。

- **TiDB Cloud Starter**：無料枠が手厚く、このデモ規模なら余裕。ベクトルも全文も無料枠内。
- **Gemini**：埋め込みも生成（flash-lite）も無料枠で収まる。
- **Vercel**：Hobby（無料）でOK。サーバー常駐もいらない。

唯一の注意は、Geminiは無料枠でもAPIキー（Google AI Studioで発行）が要ること、くらいですね。

---

## 10. まとめ / これから

TiDBのハイブリッド検索で「応募者を“合う順”にランキングして、AIが理由を言う」ところまで作れました。やってみて思ったのは、

- **1つのDB・1テーブル**でベクトル検索と全文検索が同居できるの、シンプルで強い（しかもSQLで完結）
- **RRF**は“順位だけ”で2つの検索を安全に混ぜられる出来合いレシピ。`?? 0` の合算で「両方ヒット」が上位に来る
- **Claude Codeと「先に定義を固めてから、自分で手を動かす」やり方**は、初挑戦の領域（ベクトル/TiDB）でも迷子にならずに進めた。「これ何？」を潰しながら進めると、ちゃんと自分の知識として残る感覚があってよかった

今後やりたい / 妄想していること：

- 履歴書の**チャンク分割**で精度アップ、**リランカー**での再ランキング
- **年収・勤務地・応募職種ID**などの構造化フィルタを `WHERE` で併用（同じDBやから楽）
- 「1テーブルでハイブリッド検索」という発想を、**AIエージェントの永続メモリ（記憶の保存・想起）**に応用する（mem9 みたいな方向）

ベクトル検索もTiDBも今回が初挑戦でしたが、「へぇ、こうなってるのか」を積み重ねたら、ちゃんと動くものができました。

勉強になった〜、良かったよかった。

同じく「ベクトル検索・TiDB気になるけど難しそう」と思ってる人の、最初の一歩のきっかけになれば嬉しいです。最後まで読んでくれてありがとうございました🙏
